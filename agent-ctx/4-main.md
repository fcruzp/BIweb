# Task 4 — Apply i18n translations to ALL user-facing components

## Agent: main
## Status: COMPLETED

## Summary
Applied i18n translations to all 17 user-facing components, replacing all hardcoded English text with `t('key')` calls using the existing `useI18n()` hook.

## Key Changes

### Infrastructure
- **`src/lib/i18n.ts`** — Expanded from ~50 to ~130 translation keys, with full EN and ES dictionaries. Added parameterized keys (`{count}`, `{current}`, `{total}`) for dynamic text.
- **`src/hooks/use-i18n.ts`** — Updated `t()` function to accept optional `params` object for `{placeholder}` replacement.

### Components Updated (17 total)
1. `chat-interface.tsx` — Ready for queries, Processing, Report, Generate report
2. `message-item.tsx` — rows, confidence, Geographic Map, SQL Query, Show/Hide Raw Data, Copied!, Copy question, Copy as Markdown, DR heat map title
3. `welcome-screen.tsx` — All feature titles/descriptions, sidebar hint, upload button
4. `message-input.tsx` — Placeholder, input hint
5. `chat-report.tsx` — Report, Print/Save PDF, Close, Question, SQL Query, Data rows, Generated by
6. `pin-to-dashboard-button.tsx` — Pin to Dashboard, Select Dashboard, empty state
7. `app-sidebar.tsx` — Navigation labels, Data Sources, Chats, Settings, app subtitle. **Added LocaleSwitcher in sidebar footer**
8. `datasource-list.tsx` — Empty state, delete confirmation
9. `datasource-upload.tsx` — Dialog title, description, drag-drop, browse, analysis note, buttons
10. `datasource-info-dialog.tsx` — Dialog title/description, AI Summary, Business Context, Tables, Relationships, Business Glossary, status badges
11. `chat-session-list.tsx` — Empty states, delete confirmation
12. `schema-explorer.tsx` — No Data Source, loading, retry, pagination, tabs, NOT NULL
13. `dashboard-view.tsx` — Dashboards, create dialog, empty states, delete, back button
14. `add-widget-dialog.tsx` — Dialog title/description, widget types, labels
15. `widget-renderer.tsx` — No data configured, no data, no text content, retry
16. `query-history.tsx` — No Data Source, no history, status, copy, re-run
17. `ai-settings-dialog.tsx` — All dialog text, providers, model, advanced settings, status. **Added language selector section with LocaleSwitcher**

## Lint Result
- 0 errors, 1 pre-existing warning (TanStack Table incompatible library)
